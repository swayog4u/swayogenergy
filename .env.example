# Example environment variables (DO NOT COMMIT real secrets)
# Client (Vite) - prefix with VITE_
VITE_GOOGLE_MAPS_API_KEY=

# Server
GEMINI_API_KEY=
NOTIFY_EMAIL=info@swayogurja.com
EMAIL_USER=
EMAIL_PASS=
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_SECURE=false

# Database
DATABASE_URL=postgres://user:pass@host:5432/dbname

# Other
NODE_ENV=production

# Notes:
# - Fill these values in your deployment provider (e.g., Vercel) rather than committing
#   them to the repository. Use the exact variable names shown above.